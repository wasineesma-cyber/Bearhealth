# รหัสผ่านสำหรับเข้าหน้าแอดมิน (/admin) — ตั้งค่าจริงก่อนใช้งาน
# ถ้าไม่ตั้ง ค่าเริ่มต้นคือ "bearcards"
ADMIN_PASSWORD=change-this-password

# secret สำหรับเข้ารหัส cookie ของแอดมิน — รัน: openssl rand -base64 32
ADMIN_SECRET=your-random-secret-here
